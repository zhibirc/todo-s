interface Project {
                     name: string;
             description?: string;
    readonly creationDate: number;
}

export class ProjectModel implements Project {
    // TODO: implements
}
